# AI Atlas — robots.txt

User-agent: *

# Block admin/quality/api endpoints from indexing or crawling.
Disallow: /admin/
Disallow: /api/
Disallow: /quality

# Block Next.js static assets (JS, CSS, fonts) — Google should never
# crawl these. Without this rule, Googlebot may encounter redirect
# variants (HTTP→HTTPS, trailing slash) and flag "Page with redirect".
Disallow: /_next/static/

Sitemap: https://ai-atlas.app/sitemap.xml

# Crawl-delay was removed 2026-09-05.
#
# It was added 2026-07-02 because every /use-cases/[id] render pulled ~1.7 MB
# from Supabase, so a full crawl of the catalogue cost ~1 GB. That cause is
# gone: the related-use-cases list is now served from a 10-minute
# unstable_cache and the catalogue queries no longer select the content
# column. Throttling every non-Google crawler to one request per 10s was
# suppressing discovery of 700+ URLs for a problem that no longer exists.
